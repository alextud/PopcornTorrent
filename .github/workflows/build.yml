name: CI

on: [push]

jobs:
  build-tvos:
    name: tvOS

    runs-on: macOS-12

    steps:                
    - name: "Run Tests"
      run: |
        xcodebuild \
        -scheme PopcornTorrent \
        -destination "platform=tvOS Simulator,OS=16.0,name=Apple TV" \
        test
